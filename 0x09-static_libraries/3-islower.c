#include "main.h"

/**
 * _islower - function that checks for lowercase character
 * @c: character
 * Return: always 0, 1 if c is lowercase
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
